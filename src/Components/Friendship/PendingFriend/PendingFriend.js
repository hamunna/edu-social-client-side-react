import { Button } from '@mui/material';
import React from 'react';

const PendingFriend = () => {
	return (
		<Button className="theme-fr-btn" sx={{ px: '3vw' }} >Request Pending</Button>

	);
};

export default PendingFriend;